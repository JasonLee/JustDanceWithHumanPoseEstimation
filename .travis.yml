language: node_js

node_js:
  - lts/*

cache: npm

script: 
  - cd frontend
  - npm install
  - npm test
  # Commented out due to MongoDB altas db cluster being disabled.
  # - cd ../backend
  # - npm install
  # - npm test